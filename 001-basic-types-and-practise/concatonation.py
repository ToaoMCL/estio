i = 1
f = 6.6
s = "S"
si = "4"

print(i+f)          # two numeric values added together
print(str(i) + s)   # an numeric variable wrapped into a string and combined with another string 
print(si + s)       # two strings combined together
print(int(si) + f)  # text wraped into a integer added to float